<?php

// Logical Operators

$foo = true;
$bar = false;

// TRUE if both operands are true
$foo and $bar;
$foo &&  $bar;

// TRUE if either operands are true
$foo or  $bar;
$foo ||  $bar;

// TRUE if either operands are true, but not both
$foo xor $bar;

// TRUE if $foo is FALSE
! $foo;